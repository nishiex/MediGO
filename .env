# MediGO frontend environment — talks to the Node/Express backend.

# Turn OFF the built-in mock layer so the app calls the real API.
VITE_USE_MOCK=false

# Base URL of the MediCheck Node backend (note: port 4000, path /api).
VITE_API_BASE_URL=http://localhost:4000/api
