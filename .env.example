# Copy this file to ".env" for local development.

# Set to "false" to call the real Node backend; anything else uses the mock layer.
VITE_USE_MOCK=false

# Base URL of the MediCheck Node backend.
VITE_API_BASE_URL=http://localhost:4000/api
